self.addEventListener('notificationclick',event=>{event.notification.close();const data=event.notification.data||{},url=new URL('./',self.registration.scope);if(data.noteId)url.searchParams.set('notification',String(data.noteId));if(data.date)url.searchParams.set('date',data.date);event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(async windows=>{if(windows[0]){await windows[0].navigate(url.href);return windows[0].focus()}return clients.openWindow(url.href)}))})
